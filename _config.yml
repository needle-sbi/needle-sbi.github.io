title: NEEDLE
email: stephen.pw.jiggins@gmail.com
description: >-
  NEEDLE - Neural Simulation-based Inference using Generative AI methods for Particle Physics at the LHC.

baseurl: "" # org/user pages deploy at the domain root
url: "https://needle-sbi.github.io"

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Collections
collections:
  team:
    output: true
    permalink: /team/:name/

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - README.md
  - QUICKSTART.md          # Solver Liquid syntax conflict, where .md example code is read by jekyll.
  - DEPLOYMENT.md          # Solver Liquid syntax conflict, where .md example code is read by jekyll.
  - CUSTOMIZATION.md       # Solver Liquid syntax conflict, where .md example code is read by jekyll.
  - STRUCTURE.md           # Solver Liquid syntax conflict, where .md example code is read by jekyll. (this is the problematic one!)
  - DESIGN.md              # Solver Liquid syntax conflict, where .md example code is read by jekyll.
  - GETTING_STARTED.md     # Solver Liquid syntax conflict, where .md example code is read by jekyll.
  - LICENSE            